-- Drop legacy homepage_hero_sliders table now superseded by sliders module

-- Remove table if it still exists (indexes/comments dropped automatically)
DROP TABLE IF EXISTS homepage_hero_sliders CASCADE;


