# IPD8 Production Environment Variables
# Copy this file to .env.production and update with your actual values
# DO NOT commit .env.production to version control

# Node Environment
NODE_ENV=production

# Server Port (should match your reverse proxy configuration)
PORT=3100

# Next.js Public Variables
# These will be exposed to the browser
NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com
NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# Authentication (if using NextAuth.js)
# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET=change-this-to-a-secure-random-string
NEXTAUTH_URL=https://yourdomain.com

# Database (if applicable)
# DATABASE_URL=postgresql://user:password@localhost:5432/ipd8_db

# External Services (if applicable)
# SMTP_SERVER=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-password

# Analytics (optional)
# GOOGLE_ANALYTICS_ID=UA-XXXXXXXXX-X

# Security
# Add any other security-related environment variables here

